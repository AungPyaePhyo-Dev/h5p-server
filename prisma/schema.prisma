generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
}

model ContentRecord {
  id          String   @id
  title       String
  mainLibrary String
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt

  @@map("h5p_content")
}
